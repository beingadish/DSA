#include<bits/stdc++.h>

using namespace std;

void solution(int arr[],int n,int k)
{
    for(int i=0;i<i+4;i++)
}

intt main()
{
    int arr[]={1,2,2,1,3,1,1,3,1};
    int size=sizeof(arr)/sizeof(arr[0]);
    int k=4;
    solution(arr,size,k);
    return 0;
}